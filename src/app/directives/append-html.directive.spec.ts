import { AppendHtmlDirective } from './append-html.directive';

describe('AppendHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new AppendHtmlDirective(el);
    expect(directive).toBeTruthy();
  });
});
